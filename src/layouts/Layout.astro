---
import '@fontsource/jetbrains-mono';
import Header from "../components/Header.astro";
interface Props {
	title: string;
	despcription?: string;
	image?: string;
}

const { title, despcription = "My personal website to show case my portfolio and background.", image = "/images/thumbnail.png" } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={despcription} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<meta property="og:image" content={image} />
		<title>{title}</title>
	</head>
	<body class="min-h-screen bg-n4xo-light">
		<Header />
		<slot />
	</body>
</html>
